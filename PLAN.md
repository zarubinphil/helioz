# Конвейер непрерывной работы — план стройки

Доктрина: `~/Desktop/Конвейер/` (анатомия ночи 22–24.08.2026 + разбор промта-оркестратора).
Эталон прибора: `~/Проекты/mnemazine/scripts/mnemazine-rebuild-gate.mjs` (только чтение).
Канал владельца: бот «Зевс» @ZeusKaifBot, секреты `~/.secrets/olympuz-telegram.env`
(`OLYMPUZ_TELEGRAM_TOKEN`, `OLYMPUZ_TELEGRAM_CHAT`) — значения читает только рантайм, в код/git/логи не попадают.

Параллельная работа: кампания «Мозг 2.0» идёт в другой сессии. Разграничение: `~/Мозг` не трогаем,
`~/Проекты/mnemazine/scripts/` только чтение, весь код — здесь. По прохождении враждебных проб —
`READY.json` + отбивка; приём чужой очереди развилок и ledger — `--adopt` без потерь.

## Фазы

| Ф | Что | Команда проверки (exit 0) | Враждебная проба | Стоп-критерий | Цена (оценка) |
|---|---|---|---|---|---|
| Ф0 | Дом, план, контракты состояния | `test -f PLAN.md && test -f docs/CONTRACTS.md && git -C . rev-parse HEAD` | план без команды проверки у фазы → фаза не принимается | — | 10 мин |
| Ф1 | `helioz-gate.mjs`: очередь задач, `--ready` (deps+пересечение путей+слоты), `--start/--finish`, маркеры целостности + tampered, `--budget`, `--beat`, STOP, `--adopt` | `node scripts/helioz-gate.mjs --selftest` | маркер руками/обрезанный → tampered; пересечение путей → второй не стартует; пустая очередь → exit 2 | selftest красный после 2 переоткрытий → разбор метода | 60 мин |
| Ф2 | `helioz-zeus.mjs`: `send` (durable outbox → best-effort Telegram), `ask` (развилка+кнопки), `poll` (ответы, «стоп», «переиграть»), `flush` | `node scripts/helioz-zeus.mjs --selftest` (стаб-HTTP) + живой `send` доехал | мёртвый endpoint → сообщение лежит в outbox, конвейер не падает; чужой chat id → тишина | Telegram API сменил контракт → заморозка фазы | 45 мин |
| Ф3 | `helioz-exec.mjs`: зонд CLI настоящим прогоном, ротация квоты, headless-запуск исполнителя/проверяющего | `node scripts/helioz-exec.mjs --selftest` (стаб-бинари) + живой зонд ≥1 CLI | сломанный invoke (`kimi -y -p`) → зонд красный, не зелёный | все 3 CLI мертвы → STOP+отбивка | 45 мин |
| Ф4 | `helioz-council.mjs`: совет Карпаты (3–5 линз, изоляция, синтез, ledger), тихие часы, отказ от 3 запретных случаев | `node scripts/helioz-council.mjs --selftest` (стаб-советники) | развилка kind=prod/foreign/expensive → совет ОТКАЗЫВАЕТСЯ (exit 2), развилка ждёт владельца | синтез без позиций советников → красный | 45 мин |
| Ф5 | Непрерывность: `helioz-watchdog.sh` (heartbeat→автоперезапуск с хэндоффом), launchd-плисты, `ORCHESTRATOR.md` (замороженный промт), README | `bash scripts/helioz-watchdog.sh --selftest` | STOP на диске → watchdog не стартует оркестратора; свежий heartbeat → не стартует | — | 30 мин |
| Ф6 | ВЕРТИКАЛЬНЫЙ СРЕЗ живьём: учебная задача → очередь → исполнитель → проверяющий → маркер → отбивка → ночная развилка → совет (живые CLI) → ledger → утренняя сводка | маркер учебной задачи done + запись совета в ledger + отбивки доставлены (outbox: delivered) | «переиграть» решение совета из Telegram → развилка переоткрыта | срез не прошёл — вширь не строить | 40 мин + токены CLI |
| Ф7 | `--probes`: шесть сквозных враждебных проб конвейера, `READY.json`, отбивка готовности, замер цены | `node scripts/helioz-gate.mjs --probes` | все шесть из ПОРЯДКА СТРОЙКИ п.3 промта | любая проба неверный цвет → READY не пишется | 30 мин |

Приёмка каждой фазы — только exit 0 команды проверки, самопроверка агента не засчитывается.
Шесть проб Ф7: (1) убить оркестратора посреди задачи → новый продолжает; (2) поддельный маркер → tampered;
(3) владелец молчит сутки → работа идёт, развилки целы; (4) Telegram недоступен → отбивки копятся и доезжают;
(5) совету запретная развилка → отложил; (6) пересекающиеся файлы → второй не стартует.

{
  "_комментарий": "Уроки П26/справки-CLI 23.08.2026: kimi -y/-auto несовместимы с -p; зонд обязан быть настоящим прогоном, не provider list; у codex нет fetch произвольного URL.",
  "claude": {
    "invoke_read": ["claude", "-p"],
    "invoke_write": ["claude", "-p", "--permission-mode", "acceptEdits"],
    "stdin_prompt": true,
    "roles": ["execute", "verify", "advise", "synthesize"],
    "web": { "search": "WebSearch", "fetch": "WebFetch" }
  },
  "codex": {
    "invoke_read": ["codex", "--ask-for-approval", "never", "exec", "--skip-git-repo-check", "-s", "read-only", "--ephemeral"],
    "invoke_write": ["codex", "--ask-for-approval", "never", "exec", "--skip-git-repo-check", "-s", "workspace-write", "--ephemeral"],
    "stdin_prompt": true,
    "roles": ["execute", "verify", "advise", "synthesize"],
    "web": { "search": "--search", "fetch": null }
  },
  "kimi": {
    "invoke_read": ["kimi", "-p"],
    "invoke_write": null,
    "_почему_без_write": "поведение kimi -p на запись без -y не проверено (справка-CLI §непроверенное); до пробы - только чтение",
    "stdin_prompt": false,
    "roles": ["verify", "advise"],
    "web": { "search": "WebSearch", "fetch": "FetchURL" }
  }
}
